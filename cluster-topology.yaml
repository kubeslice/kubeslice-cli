configuration:
  cluster_configuration:
    controller:
      name: controller
      context_name: kind-ks-ctrl
      kube_config_path: controller-config.yaml
    # workers:
    #   - name: kind-ks-w-1
    #     context_name: kind-ks-w-1
    #     kube_config_path: worker-config.yaml
  kubeslice_configuration:
    project_name: preprod
  helm_chart_configuration:
    repo_alias: kubeslice
    repo_url: https://kubeslice.github.io/kubeslice/
    cert_manager_chart:
      chart_name: cert-manager
    controller_chart:
      chart_name: kubeslice-controller
    worker_chart:
      chart_name: kubeslice-worker